# ERC-721 学习项目（Foundry + Soldeer）

依赖由 **Soldeer** 安装到 `dependencies/`，与课内 `class-7/my-foundry-project` 同款思路。

从零步骤、概念、Sepolia 部署与验证见：

**[`md/draf/LearnNFT-ERC721与Soldeer及Sepolia验证攻略.md`](md/draf/LearnNFT-ERC721与Soldeer及Sepolia验证攻略.md)**

首次拉依赖：

```shell
cd class-10/erc721-learn
forge soldeer install
cp .env.example .env
forge build && forge test
```
